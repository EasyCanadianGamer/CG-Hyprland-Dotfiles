-- Programs used across other config files.
-- require("programs") from anywhere in ~/.config/hypr to get these.

return {
	terminal = "kitty",
	fileManager = "dolphin",
	browser = "firefox",
	helium = "helium-browser",
	menu = "rofi -show drun -theme ~/.config/rofi/launchers/type-5/style-4",
}
